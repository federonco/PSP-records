## PSP Record Sheet Test Checklist

- CH 2550 passes validation and lodges.
- Direction toggle + +/- step 20 correctly.
- Duplicate shows "Already recorded" and overwrite uses dialog confirm.
- Lodge succeeds with `signature_strokes = null`.
- Signature saved as strokes and preview renders on Index.
- Sections create/rename and persist per location.
- Admin route requires auth before loading data.
- Admin page card order matches Section/Records/Blocks/Audit/Sign-Off.
